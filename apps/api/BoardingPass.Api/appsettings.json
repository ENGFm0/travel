{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Cors": {
    "Origins": ["http://localhost:5173", "http://localhost:4173"]
  },
  "_comment": "Secrets (Firebase Admin credentials, AviationStack/Maps/Exchange keys) are NOT stored here. Use environment variables / secret store (Key Vault) — architecture §6."
}
