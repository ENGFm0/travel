{
  "//": "Root config for CI. Hosting serves the built web app (apps/web/dist) as an SPA. Firestore rules/indexes are kept in firebase/ and deployed from here too.",
  "hosting": {
    "public": "apps/web/dist",
    "ignore": ["firebase.json", "**/.*", "**/node_modules/**"],
    "rewrites": [{ "source": "**", "destination": "/index.html" }]
  },
  "firestore": {
    "rules": "firebase/firestore.rules",
    "indexes": "firebase/firestore.indexes.json"
  }
}
